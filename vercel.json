{
  "framework": "vite",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @micro-ats/web build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}